{
  "name": "poe2-price-checker-root",
  "version": "1.0.2",
  "private": true,
  "description": "Root workspace runner for Poe2 Price Checker",
  "scripts": {
    "install:all": "npm install --prefix renderer && npm install --prefix main",
    "dev:renderer": "npm run dev --prefix renderer",
    "dev:main": "npm run dev --prefix main",
    "dev:standalone": "npm run dev --prefix main -- -- --standalone",
    "make-index-files": "npm run make-index-files --prefix renderer"
  }
}
